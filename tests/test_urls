from django.test import SimpleTestCase
from message_board.views import messageBoard, Messages, MainMessages, ManageClubs, ManageUsers
from django.urls import reverse, resolve

class TestUrls(SimpleTestCase):

    def test_messageBoard_url_resolves(self):
        assert 1==2
        #url = reverse('admins')
        #print(resolve(url))
        #self.assertEqual(resolve(url).func,'message_board/Admin_message_board.html' )